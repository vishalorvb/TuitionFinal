from django.urls import include, path,re_path
from usermanager import views
app_name = 'usermanager'
urlpatterns=[
    # re_path(r'^registration',views.registration,name='registration'),
    # re_path(r'^login_page',views.login_page, name='login_page'),
    # re_path(r'^verify_otp',views.verify_otp,name='verify_otp'),
    # re_path(r'^test_login',views.test_login,name='test_login'),
    # re_path(r'^logout',views.user_logout,name='logout'),
    # re_path(r'^profile',views.user_profile,name='profile'),
]
